export const Navbar = () => {
  return (
    <div className="navbar w-[90vw] bg-base-100 shadow-sm justify-between">
        <a className="btn btn-ghost text-xl">Gallery</a>
        <button>Logout</button>
    </div>
  )
}
